# .env — docker-compose variable defaults
# =============================================================================
# DISPLAY: Points to the XQuartz socket on macOS.
#   host.docker.internal is Docker Desktop's magic DNS for the host machine.
#   :0 is the default X11 display number XQuartz listens on.
# =============================================================================
DISPLAY=host.docker.internal:0